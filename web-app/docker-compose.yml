version: "3"
services:
  postgres:
    image: postgres
    container_name: licence_image_processing
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=imageprocessing
    ports:
      - 5432:5432
    volumes:
      - ./data:/var/lib/postgresql/data